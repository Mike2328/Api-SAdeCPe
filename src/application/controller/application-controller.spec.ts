import { ApplicationController } from './application-controller';

describe('ApplicationController', () => {
  it('should be defined', () => {
    expect(new ApplicationController()).toBeDefined();
  });
});
